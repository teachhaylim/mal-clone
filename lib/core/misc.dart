// ignore_for_file: constant_identifier_names

import 'package:retrofit/dio.dart';

///* Misc File usually used to store unrelated or un-groupable functions, classes, enum, etc..

const REQUIRED_AUTH_KEY = "requiredAuthentication";

const RequiredAuthentication = Extra({REQUIRED_AUTH_KEY: true});
