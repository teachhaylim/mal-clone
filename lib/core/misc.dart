import 'package:retrofit/dio.dart';

///* Misc File usually used to store unrelated or un-groupable functions, classes, enum, etc..

const requiredAuthenticationKey = "requiredAuthentication";

// ignore: constant_identifier_names
const RequiredAuthentication = Extra({requiredAuthenticationKey: true});
