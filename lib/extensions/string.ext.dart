extension StringExt on String {
  // String get toShorten {
  //   if (length < 3) return this;

  //   if (length >= 3 && length <= 6) return "${length / 3} K";

  //   if (length >= 6 && length <= 9) return "${length / 6} M";

  //   if (length >= 9) return "${length / 9} B";

  //   return this;
  // }
}
